//Qui vanno i principali comandi JS

// Guess the number (giocato da un bot)

/*
let rand = () => {
    return Math.round(Math.random() * 10);
}
let n, i = 0;
const NUM = rand();
console.log('Numero pensato: %d\n', NUM);
do {
    i++;
    n = rand();
    if (n != NUM) {
        console.log('%d: Sbagliato!', n);
    }
} while (n != NUM);

console.log("\n%d: GIUSTO !",n);
if (i==1) {
    console.log('Trovato in %d tentativo',i);
} else {
    console.log('Trovato in %d tentativi',i);
}
*/